/**
 * @typedef {[number, number, number, number?]} RGB
 * @typedef {[number, number, number, number?]} HSL
 * @typedef {[number, number, number]} HSV
 * @typedef {[number, number, number]} HWB
 * @typedef {[number, number, number, number]} CMYK
 * @typedef {[number, number, number]} XYZ
 * @typedef {[number, number, number]} Lab
 * @typedef {[number, number, number]} LCh
 */

export {};
